<% layout('layout') %>

<section>
  <h2><%= title %></h2>
  <p><%= description %></p>

  <ul>
    <% features.forEach(feature => { %>
      <li><%= feature %></li>
    <% }) %>
  </ul>
</section>

